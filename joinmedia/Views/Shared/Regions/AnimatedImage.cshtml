@model AnimatedImage

@if (Model != null && !Model.Hide)
{
	@*//////////////////////////////////////////////////////////////////////////////////////
	CODE *@

	//
	
	@*//////////////////////////////////////////////////////////////////////////////////////
	HTML *@
	
	@Html.Partial("Image/_PictureFill_EffectsAnimatedImage", PictureFillEffectModel.Get(Model))
}
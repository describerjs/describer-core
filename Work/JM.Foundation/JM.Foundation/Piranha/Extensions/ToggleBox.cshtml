@using System.Web.Mvc.Html
@model ToggleBox

<ul class="form">
	<li>
		@Html.LabelFor(m => m.Headline, "Überschrift")
		<div class="input">
		    @Html.TextBoxFor(m => m.Headline)
	    </div>
		<p>Die Überschrift ist optional und kann leer bleiben.</p>
	</li>
	<li>
		@Html.LabelFor(m => m.Content)
		<div class="input">
			@Html.TextAreaFor(m => m.Content, new { rows=8, cols=20, @class="editor"})
		</div>
	</li>
	<li>
		@Html.LabelFor(m => m.Hide, "Verstecken")
		<div class="input">@Html.CheckBoxFor(m => m.Hide)</div>
		<p>Blendet das aktuelle Element vollständig aus</p>
	</li>
</ul>

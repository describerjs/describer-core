#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-asset-revisions/bin/grunt-asset-revisions" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-asset-revisions/bin/grunt-asset-revisions" "$@"
  ret=$?
fi
exit $ret

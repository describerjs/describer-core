#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../fb-flo/bin/flo" "$@"
  ret=$?
else 
  node  "$basedir/../fb-flo/bin/flo" "$@"
  ret=$?
fi
exit $ret
